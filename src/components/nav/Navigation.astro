---
import NavBar from './nav_bar/NavBar.astro';
---

<NavBar />

<script is:inline>
  // Navigation component with updated NavBar that no longer includes overlay menu
  // The JavaScript for the overlay menu has been removed as requested
  // This component now just renders the updated NavBar without menu overlay functionality