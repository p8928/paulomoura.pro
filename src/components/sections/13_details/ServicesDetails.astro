---
import Button from '../../ui/Button.astro';
import InteractiveServices from './InteractiveServices.astro';

// Combinations data - can be moved or fetched from a CMS later
const combinations = [
  {
    title: 'Para Empresas Iniciando:',
    description: 'Branding Digital + Web Experience - Base sólida de identidade + presença digital'
  },
  {
    title: 'Para Crescimento Orgânico:',
    description: 'Brand-Driven SEO + Growth Strategy - Visibilidade sustentável + roadmap de crescimento'
  },
  {
    title: 'Para Transformação Completa:',
    description: 'Branding Digital + Web Experience + Brand-Driven SEO - Identidade + presença técnica + autoridade orgânica'
  },
  {
    title: 'Para Decisões Estratégicas:',
    description: 'Trust Advisement (standalone) - Consultoria focada em decisão específica'
  }
];
---

<!-- Services Hero Section matching Metodologia style -->
<div class="metodology-hero">
  <header class="metodology-header">
    <h1 class="metodology-title">
      <span class="title-brand">SERVIÇOS</span>
    </h1>
  </header>
</div>

<script is:inline type="module">
  // Initialize GSAP and ScrollTrigger after ensuring they are loaded
  const initServiceAnimations = async () => {
    try {
      // Dynamically import GSAP and ScrollTrigger
      const [{ default: gsap }, { ScrollTrigger }] = await Promise.all([
        await import('https://esm.sh/gsap@3.13.0'),
        await import('https://esm.sh/gsap/ScrollTrigger?bundle=esm')
      ]);

      gsap.registerPlugin(ScrollTrigger);

      const initServiceHeroAnimations = () => {
        const title = document.querySelector('.metodology-title');
        if (title) {
          // Entrance animation
          gsap.fromTo(title, 
            { x: -50, opacity: 0 }, 
            { x: 0, opacity: 1, duration: 1, ease: "power3.out", delay: 0.2 }
          );

          // Parallax effect
          gsap.to(title, {
            y: "30%",
            ease: "none",
            scrollTrigger: {
              trigger: ".metodology-hero",
              start: "top top",
              end: "bottom top",
              scrub: true
            }
          });
        }

        // Overview section title line animation
        const overviewTitle = document.querySelector('.overview-title');
        if (overviewTitle) {
          // Add a CSS class to trigger the pseudo-element animation using CSS
          gsap.to(overviewTitle, { 
            scrollTrigger: {
              trigger: ".services-overview",
              start: "top 80%", // Trigger when 80% of the section is visible
              toggleActions: "play none none none"
            },
            onComplete: () => {
              // Add a class to trigger the pseudo-element animation
              overviewTitle.classList.add('animate-line');
            }
          });
        }
      };

      // Execute immediately when DOM is ready
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initServiceHeroAnimations);
      } else {
        initServiceHeroAnimations();
      }

      // Executar em navegações subsequentes do Astro
      document.addEventListener('astro:page-load', initServiceHeroAnimations);
      
      // Limpar os ScrollTriggers ao sair da página (importante para navegação do Astro)
      document.addEventListener('astro:before-swap', () => {
        ScrollTrigger.getAll().forEach(trigger => trigger.kill());
      });
    } catch (err) {
      console.warn('GSAP or ScrollTrigger could not be loaded. Animations will not run.', err);
    }
  };
  
  // Execute the animation initialization
  initServiceAnimations();
</script>

<!-- Services Overview Section -->
<section class="services-overview">
  <div class="overview-grid">
    <div class="overview-title-container">
      <h2 class="overview-title">Arquitetura Digital. Autoridade Duradoura.</h2>
    </div>
    <div class="overview-description-container">
      <p class="overview-description">
        Nossos serviços são os componentes de uma presença digital coesa. Guiados pela metodologia brand-driven, construímos sistemas — não campanhas. Cada projeto é uma solução customizada, projetada para refletir sua identidade, gerar resultados e evoluir com o tempo.
      </p>
    </div>
  </div>
</section>

<!-- Interactive Services Section -->
<InteractiveServices />

<!-- Architectural Content Sections -->
<div class="architectural-wrapper">

  <!-- Process Section (Formerly "Nossa Abordagem") -->
  <section class="process-section">
    <div class="process-grid">
      <div class="process-title-container">
        <h3 class="process-title">PROCESSO</h3>
      </div>
      <div class="process-steps">
        <div class="step">
          <span class="step-number">01</span>
          <h4 class="step-title">Descoberta</h4>
          <p class="step-description">Entendemos profundamente seu negócio, identidade, desafios e objetivos para construir uma base estratégica sólida.</p>
        </div>
        <div class="step">
          <span class="step-number">02</span>
          <h4 class="step-title">Estratégia</h4>
          <p class="step-description">Definimos o roadmap, com objetivos claros e um plano de ação priorizado para alcançar os resultados esperados.</p>
        </div>
        <div class="step">
          <span class="step-number">03</span>
          <h4 class="step-title">Execução</h4>
          <p class="step-description">Implementamos a estratégia com excelência técnica e criativa, garantindo que cada detalhe reflita sua marca.</p>
        </div>
        <div class="step">
          <span class="step-number">04</span>
          <h4 class="step-title">Evolução</h4>
          <p class="step-description">Acompanhamos os resultados, analisamos os dados e otimizamos continuamente para garantir o crescimento sustentável.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Combinations Section -->
  <section class="combinations-section">
    <h3 class="section-title">Combinações Estratégicas</h3>
    <ul class="combinations-list">
      {combinations.map(combo => (
        <li class="combination-item">
          <h4 class="combination-title">{combo.title}</h4>
          <p class="combination-description">{combo.description}</p>
        </li>
      ))}
    </ul>
  </section>

</div>

<!-- Final CTA Section -->
<section class="final-cta-section">
  <div class="final-cta-content">
    <h3 class="final-cta-title">Vamos construir algo duradouro.</h3>
    <p class="final-cta-description">
      Agende uma conversa para desenharmos a estratégia ideal para o seu negócio.
    </p>
    <Button href="/contato" variant="secondary">
      Iniciar conversa
      <span class="arrow">→</span>
    </Button>
  </div>
</section>


<style>
/* Hero Section */
.metodology-hero {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100vh;
  background-color: var(--color-background);
  overflow: hidden;
  position: relative; /* Needed for ScrollTrigger */
}

.metodology-header {
  text-align: center;
  position: relative; /* Needed for Parallax */
  z-index: 1;
}


  .metodology-title {
    font-family: var(--font-family-heading);
    font-size: var(--font-size-hero);
    font-weight: var(--font-weight-bold);
    text-transform: uppercase;
    line-height: 1;
    margin: 0;
    will-change: transform; /* Performance hint for animations */
  }

  /* Overview Section - Asymmetric Grid */
  .services-overview {
    padding: var(--space-5xl) var(--space-lg);
    background-color: var(--color-background);
    position: relative;
    z-index: 2;
  }

  .overview-grid {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1.5fr; /* Asymmetric ratio */
    gap: var(--space-4xl);
    align-items: start;
  }

  .overview-title-container {
    position: relative;
    padding-bottom: var(--space-md);
  }

  .overview-title {
    font-size: var(--font-size-4xl);
    text-align: left;
  }

  .overview-title::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background-color: var(--color-accent);
    transform-origin: left;
    transform: scaleX(0); /* Initially hidden for animation */
    transition: transform 1.2s cubic-bezier(0.34, 1.56, 0.64, 1); /* Smooth cubic-bezier for the line animation */
  }
  
  .overview-title.animate-line::after {
    transform: scaleX(1);
  }

  .overview-description-container {
    border-left: 1px solid var(--color-border);
    padding-left: var(--space-4xl);
  }

  .overview-description {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    max-width: 65ch;
    line-height: var(--line-height-relaxed);
    text-align: left;
    margin: 0;
  }
  
  @media (max-width: 768px) {
    .overview-grid {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
    }
    .overview-description-container {
      border-left: none;
      padding-left: 0;
    }
    .overview-title {
      text-align: center;
    }
     .overview-title::after {
      left: 50%;
      transform: translateX(-50%) scaleX(0);
    }
    .overview-description {
      text-align: center;
    }
  }



/* Architectural Wrapper */
.architectural-wrapper {
  max-width: 1200px;
  margin: var(--space-5xl) auto;
  padding: 0 var(--space-lg);
}

/* Common Section Title */
.section-title {
  font-size: var(--font-size-4xl);
  font-weight: var(--font-weight-medium);
  text-align: center;
  margin-bottom: var(--space-3xl);
  letter-spacing: var(--letter-spacing-tight);
}

/* Process Section */
.process-section {
  margin-bottom: var(--space-5xl);
}

.process-grid {
  display: grid;
  grid-template-columns: 1fr 3fr;
  gap: var(--space-4xl);
  align-items: start;
}

.process-title-container {
  text-align: right;
  position: sticky;
  top: 150px;
}

.process-title {
  font-size: var(--font-size-xl);
  font-weight: var(--font-weight-bold);
  color: var(--color-text-tertiary);
  text-transform: uppercase;
  letter-spacing: var(--letter-spacing-wider);
  writing-mode: vertical-rl;
  transform: rotate(180deg);
  margin: 0;
  padding: var(--space-lg) 0;
}

.process-steps {
  display: flex;
  flex-direction: column;
  gap: var(--space-3xl);
}

.step {
  border-left: 2px solid var(--color-border);
  padding-left: var(--space-xl);
}

.step-number {
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-bold);
  color: var(--color-accent-accessible);
  display: block;
  margin-bottom: var(--space-sm);
}

.step-title {
  font-size: var(--font-size-2xl);
  font-weight: var(--font-weight-medium);
  margin-bottom: var(--space-md);
}

.step-description {
  font-size: var(--font-size-lg);
  color: var(--color-text-secondary);
  max-width: 60ch;
  line-height: var(--line-height-relaxed);
}

/* Combinations Section */
.combinations-list {
  list-style: none;
  padding: 0;
  margifn: 0;
  border-top: 1px solid var(--color-border);
}

.combination-item {
  display: grid;
  grid-template-columns: 1fr 2fr;
  gap: var(--space-xl);
  padding: var(--space-xl) 0;
  border-bottom: 1px solid var(--color-border);
  align-items: center;
}

.combination-title {
  font-size: var(--font-size-lg);
  font-weight: var(--font-weight-bold);
  margin: 0;
}

.combination-description {
  font-size: var(--font-size-lg);
  color: var(--color-text-secondary);
  margin: 0;
}

/* Final CTA Section */
.final-cta-section {
  background-color: #F5F5F3; /* Warm Gray */
  padding: var(--space-5xl) var(--space-lg);
  text-align: center;
  margin-top: var(--space-5xl);
}

.final-cta-title {
  font-size: var(--font-size-4xl);
  margin-bottom: var(--space-md);
}

.final-cta-description {
  font-size: var(--font-size-lg);
  color: var(--color-text-secondary);
  margin-bottom: var(--space-2xl);
}

/* Enhanced CSS to match homepage button styling */
  .final-cta-section .btn-secondary {
    padding: var(--space-md) var(--space-2xl);
    font-family: var(--font-family-primary);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-bold);
    text-transform: uppercase;
    letter-spacing: var(--letter-spacing-wider);
    border-radius: 0;
    position: relative;
    overflow: hidden;
  }

  .final-cta-section .btn-secondary::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: var(--color-accent);
    transition: left 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    z-index: -1;
  }

  .final-cta-section .arrow {
    margin-left: var(--space-sm);
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .final-cta-section .btn-secondary:hover::before {
    left: 0;
  }

  .final-cta-section .btn-secondary:hover {
    color: var(--color-primary) !important;
    border-color: var(--color-accent) !important;
    transform: translateY(-2px) !important;
    box-shadow: 0 4px 12px rgba(192, 178, 131, 0.3) !important;
  }

  .final-cta-section .btn-secondary:hover .arrow {
    transform: translateX(5px);
  }

/* Responsive Adjustments */
@media (max-width: 768px) {
  .process-grid {
    grid-template-columns: 1fr;
  }
  .process-title-container {
    position: static;
    text-align: left;
    margin-bottom: var(--space-xl);
  }
  .process-title {
    writing-mode: horizontal-tb;
    transform: none;
    padding: 0;
  }
  .combination-item {
    grid-template-columns: 1fr;
    gap: var(--space-sm);
  }
}

@media (max-width: 480px) {
    .architectural-wrapper {
        margin: var(--space-4xl) auto;
    }
    .section-title, .final-cta-title {
        font-size: var(--font-size-3xl);
    }
}

</style>
</file>
