---
import Button from '../../ui/Button.astro';
---

<section class="architecture-section animate-on-scroll">
  <div class="minimalist-overlay"></div>
  
  <div class="container">
    <div class="content-wrapper">
      <div class="left-content">
        <div class="section-header">
          <h2 class="text-title-section">
            RELEVANTE. MEMORÁVEL.
          </h2>
          
          <div class="bauhaus-element"></div>
        </div>
        
        <div class="description-block">
          <p class="text-body">
            A genuinidade da sua marca ganha clareza com design atemporal e conteúdo estratégico. A identificação que o cliente busca. A substância da qual algoritmos precisam para tomar decisões certas.
          </p>
        </div>
        
        <div class="spacing-divider"></div>
        
        <div class="cta-section">
          <Button href="#" variant="secondary">
            Confira o nosso manifesto
            <span class="arrow" style="margin-left: var(--space-sm); transition: transform 0.3s ease;">→</span>
          </Button>
        </div>
      </div>
      
      <div class="right-content">
        <div class="typographic-art">
          <div class="letter-row">
            <span class="letter">A</span>
            <span class="letter">R</span>
            <span class="letter">Q</span>
          </div>
          <div class="letter-row">
            <span class="letter">U</span>
            <span class="letter">I</span>
            <span class="letter">T</span>
          </div>
          <div class="letter-row">
            <span class="letter">E</span>
            <span class="letter">T</span>
            <span class="letter">U</span>
          </div>
          <div class="letter-row">
            <span class="letter">R</span>
            <span class="letter">A</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .architecture-section {
    background-color: #ffffff;
    padding: 12rem 2rem;
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    scroll-snap-align: start;
    overflow-x: hidden;
    border-top: 1px solid rgba(0, 0, 0, 0.05);
  }

  .minimalist-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.01) 0%, rgba(0, 0, 0, 0) 100%);
    z-index: 1;
    pointer-events: none;
  }

  .container {
    max-width: 1400px;
    width: 100%;
    margin: 0 auto;
    position: relative;
    z-index: 2;
  }

  .content-wrapper {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6rem;
    align-items: center;
  }

  .left-content {
    max-width: 600px;
  }

  .section-header {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .section-title {
  }

  .bauhaus-element {
    width: 80px;
    height: 3px;
    background: var(--color-accent);
    align-self: flex-start;
  }

  .description-block {
    margin: 3rem 5rem;
  }

  .description {
  }

  .description strong {
    color: #000000;
    font-weight: var(--font-weight-medium);
  }

  .spacing-divider {
    width: 100%;
    height: 1px;
    background: var(--color-accent);
    opacity: 0.3; /* Adicionado para suavizar */
    margin: 2rem 0;
  }

  .cta-section {
    margin-top: 2rem;
  }

  .cta-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-md) var(--space-2xl);
    background: transparent;
    color: var(--color-text-primary);
    border: 2px solid var(--color-primary);
    font-family: var(--font-family-primary);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-bold);
    cursor: pointer;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    text-decoration: none;
    white-space: nowrap;
    text-transform: uppercase;
    letter-spacing: var(--letter-spacing-wider);
    border-radius: 0;
    position: relative;
    overflow: hidden;
  }

  .cta-link::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: var(--color-accent);
    transition: left 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    z-index: -1;
  }

  .cta-link:hover::before {
    left: 0;
  }

  .cta-link:hover {
    color: var(--color-primary);
    border-color: var(--color-accent);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(192, 178, 131, 0.3);
  }

  .arrow {
    margin-left: var(--space-sm);
    transition: transform 0.3s ease;
  }

  .cta-link:hover .arrow {
    transform: translateX(5px);
  }

  .right-content {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100%;
  }

  .typographic-art {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .letter-row {
    display: flex;
    gap: 1rem;
  }

  .letter {
    font-family: var(--font-family-heading);
    font-weight: var(--font-weight-light);
    font-size: clamp(4rem, 6vw, 7rem);
    color: #000000;
    line-height: 0.85;
    letter-spacing: -0.05em;
    text-transform: uppercase;
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 1.2em;
    min-height: 1.2em;
  }

  /* Animações de entrada */
  .section-header {
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 0.8s ease-out 0.2s forwards;
  }

  .description-block {
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 0.8s ease-out 0.4s forwards;
  }

  .cta-section {
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 0.8s ease-out 0.6s forwards;
  }

  .typographic-art {
    opacity: 0;
    transform: translateX(50px);
    animation: fadeInUp 0.8s ease-out 0.8s forwards;
  }

  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0) translateX(0);
    }
  }

  /* Responsividade */
  @media (max-width: 1024px) {
    .architecture-section {
      padding: 10rem 1.5rem;
    }
    
    .content-wrapper {
      gap: 4rem;
    }

    .left-content {
      max-width: 100%;
    }

    .description-block {
      margin: 3rem 0;
    }
  }

  @media (max-width: 768px) {
    .architecture-section {
      padding: 8rem 1.5rem;
      min-height: auto;
    }

    .content-wrapper {
      grid-template-columns: 1fr;
      gap: 4rem;
    }
    
    .right-content {
      display: none;
    }
    
    .typographic-art {
      display: none;
    }

    .cta-section {
      margin-top: 2rem;
    }
    
    .section-header {
      text-align: center;
      align-items: center;
    }
    
    .bauhaus-element {
      align-self: center;
    }
  }

  @media (max-width: 480px) {
    .architecture-section {
      padding: 6rem 1rem;
    }

    .section-header {
      gap: 1.5rem;
    }
    
    .bauhaus-element {
      width: 60px;
      height: 2px;
    }
    
    .letter {
      font-size: clamp(3rem, 10vw, 5rem);
    }
    
    .description-block {
      margin: 1.5rem 0;
    }
    
    .spacing-divider {
      margin: 1.5rem 0;
    }
  }
