<section id="sobre" class="py-24 sm:py-32 px-4 sm:px-8">
  <div class="max-w-4xl mx-auto text-center">
    <h2 class="font-serif text-4xl sm:text-5xl lg:text-6xl" style="line-height: 1.5 !important;">Atendimento ágil e descomplicado</h2>
    <p class="text-sm sm:text-lg leading-loose font-normal mt-8 max-w-3xl mx-auto">Em até três etapas, explicamos suas possibilidades de forma clara.</p>
    
    <div class="journey-container mt-20">
      <!-- Step 1 -->
      <div class="journey-step">
        <div class="step-milestone">01</div>
        <div class="step-content">
          <h3 class="font-outfit text-xl mb-2">Conhecimento</h3>
          <p class="font-outfit text-sm font-normal">Você nos conta sua história e explica suas reivindicações.</p>
        </div>
      </div>
      <!-- Step 2 -->
      <div class="journey-step">
        <div class="step-milestone">02</div>
        <div class="step-content">
          <h3 class="font-outfit text-xl mb-2">Análise</h3>
          <p class="font-outfit text-sm font-normal">Analisaremos os fatos para verificar seus direitos e estudar soluções.</p>
        </div>
      </div>
      <!-- Step 3 -->
      <div class="journey-step">
        <div class="step-milestone">03</div>
        <div class="step-content">
          <h3 class="font-outfit text-xl mb-2">Ação</h3>
          <p class="font-outfit text-sm font-normal">Com sua autorização, iniciamos o processo seguindo a melhor solução encontrada.</p>
        </div>
      </div>
    </div>

    <div class="mt-20">
      <a href="#contato" class="btn-hygge">
        DÊ O PRIMEIRO PASSO
      </a>
    </div>
  </div>
</section>

<style>
  /* Journey Section Styles */
  .journey-container {
    position: relative;
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
  }

  .journey-container::before {
    content: '';
    position: absolute;
    top: 2rem; /* Half the height of the milestone (4rem / 2) */
    left: 5%;
    width: 0%;
    height: 2px;
    background-image: linear-gradient(to right, rgba(184, 134, 11, 0.2) 33%, rgba(255,255,255,0) 0%);
    background-position: bottom;
    background-size: 12px 2px;
    background-repeat: repeat-x;
    z-index: 0;
    animation: draw-line-horizontal 1.5s 0.5s ease-out forwards;
  }

  @keyframes draw-line-horizontal {
    to { width: 90%; }
  }

  .journey-step {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 30%;
    opacity: 0;
    transform: translateY(20px);
    animation: fade-in-up 0.8s ease-out forwards;
  }

  /* Staggered animation delays */
  .journey-step:nth-child(1) { animation-delay: 0.8s; }
  .journey-step:nth-child(2) { animation-delay: 1.2s; }
  .journey-step:nth-child(3) { animation-delay: 1.6s; }

  @keyframes fade-in-up {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .step-milestone {
    width: 4rem; /* 64px */
    height: 4rem; /* 64px */
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 9999px;
    border: 1px solid rgba(184, 134, 11, 0.3);
    background-color: #F3F0E9; /* Opaque background to hide the line */
    font-family: 'Playfair Display', serif;
    font-size: 1.25rem; /* 20px */
    color: #B8860B;
    transition: all 0.3s ease-in-out;
    box-shadow: 0 4px 15px rgba(0,0,0,0.05);
    position: relative;
    z-index: 1; /* Ensure milestone is above the line */
  }

  .step-content {
    margin-top: 1.5rem; /* 24px */
    transition: all 0.3s ease-in-out;
    text-align: center;
  }

  .journey-container:hover .journey-step:not(:hover) .step-milestone,
  .journey-container:hover .journey-step:not(:hover) .step-content {
    opacity: 0.5;
  }

  .journey-step:hover .step-milestone {
    background-color: #B8860B;
    color: white;
    transform: scale(1.1);
    box-shadow: 0 8px 25px rgba(184, 134, 11, 0.3);
  }

  /* Responsive Journey Section */
  @media (max-width: 767px) {
    .journey-container {
      flex-direction: column;
      align-items: center;
    }



    .journey-container::before {
      animation-name: draw-line-vertical;
      left: 50%;
      transform: translateX(-1px);
      top: 2rem;
      width: 2px;
      height: 0;
      background-image: linear-gradient(to bottom, rgba(184, 134, 11, 0.4) 33%, rgba(255,255,255,0) 0%);
      background-size: 2px 12px;
      background-repeat: repeat-y;
    }

    @keyframes draw-line-vertical {
      to { height: calc(100% - 4rem); opacity: 0.2}
    }

    .journey-step {
      width: 90%;
      max-width: 400px;
      margin-bottom: 2.5rem; /* 40px */
    }

    .journey-step:last-child {
      margin-bottom: 0;
    }
  }
</style>
