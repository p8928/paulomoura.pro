---
import HSBadge from '@components/assets/HSBadge.svelte';
import MIDBadge from '@components/assets/MIDBadge.svelte';
import MStarBadge from '@components/assets/MStarBadge.svelte';
import MSupremeBadge from '@components/assets/MSupremeBadge.svelte';
import HeroNavigation from '@components/navigation/HeroNavigation.svelte';
import SiteFooter from '@components/site/SiteFooter.astro';
import { badgeStandards } from '@data/badgeStandards';
import BaseLayout from '@layouts/BaseLayout.astro';

const qualityBadges = badgeStandards;

const systemPillars = [
  {
    label: 'Marca',
    title: 'Reconhecimento',
    text: 'Traços próprios: posicionamento, linguagem, estética, promessa e forma de atender.',
  },
  {
    label: 'Comunicação',
    title: 'Compreensão',
    text: 'Clareza sobre o que a empresa faz, para quem faz, quando importa e por que deve ser considerada.',
  },
  {
    label: 'Experiência',
    title: 'Confiança',
    text: 'Reputação, avaliações, consistência de informações e sofisticação proporcional ao nível da entrega.',
  },
  {
    label: 'Performance',
    title: 'Demanda qualificada',
    text: 'A presença passa a atrair menos tráfego indiferente e mais pessoas com contexto, intenção e condições reais de avançar.',
  },
];

const methodSteps = [
  { title: 'Interpretar a operação', detail: 'Atendimento, entrega, reputação e provas reais.' },
  { title: 'Distinguir evidência de impressão', detail: 'Identificar o que sustenta autoridade e o que apenas produz aparência de diferenciação.' },
  { title: 'Organizar em presença', detail: 'Site, conteúdo, dados e experiência sob a mesma leitura do negócio.' },
  { title: 'Tornar reconhecível', detail: 'Tornar os critérios de escolha legíveis para clientes, Google e IA.' },
];

const distinctions = [
  {
    title: 'Direção autoral',
    detail: 'A estratégia preserva a leitura central de Paulo Moura, sem linha de produção ou transferência automática de execução.',
  },
  {
    title: 'Interpretação antes da técnica',
    detail: 'Tecnologia entra para dar precisão ao que foi compreendido no negócio, na operação e na experiência real do cliente.',
  },
  {
    title: 'Lógica própria de presença',
    detail: 'Marca, conteúdo, interface, busca e reputação passam a responder ao mesmo critério, sem dependência de modelos genéricos.',
  },
  {
    title: 'Profundidade proporcional',
    detail: 'O nível de personalização acompanha exigência, maturidade e potencial real do projeto.',
  },
];
---

<BaseLayout
  title="Moura | Assinatura e método para presença digital"
  description="Moura é assinatura e método para presença digital de alto desempenho: marca, comunicação, experiência, busca e reputação sob uma mesma direção."
  canonicalPath="/moura"
  schemaPageType="AboutPage"
  showChrome={false}
>
  <script>
    import '@scripts/lenis';
  </script>

  <HeroNavigation client:load />

  <main class="moura-page">
    <section class="moura-hero" data-nav-surface="dark" aria-labelledby="moura-title">
      <div class="moura-shell moura-hero-grid">
        <div class="moura-hero-copy">
          <p class="moura-kicker">Assinatura e método</p>
          <h1 id="moura-title">Presença digital construída a partir do que torna uma empresa digna de escolha.</h1>
        </div>

        <div class="moura-hero-aside">
          <div class="moura-hero-luxury-mark" aria-label="Monograma Moura Signature">
            <MSupremeBadge scale={1.08} label="Signature" tone="gold" />
          </div>
          <p class="moura-hero-text">
            Paulo Moura conduz a interpretação do negócio, identifica evidências reais de autoridade e organiza marca, site, conteúdo, busca e reputação para que clientes, Google e IA compreendam com mais precisão por que aquela empresa merece ser escolhida.
          </p>
        </div>
      </div>

      <aside class="moura-hero-system" aria-label="Coordenadas da assinatura Moura">
        <p>01 / Assinatura Moura</p>
        <span>Leitura do negócio</span>
        <span>Direção de presença</span>
        <span>Autoridade reconhecível</span>
      </aside>
    </section>

    <section class="moura-system" data-nav-surface="light" aria-labelledby="moura-system-title">
      <div class="moura-shell moura-two-column">
        <div class="moura-section-heading">
          <p class="moura-kicker moura-kicker-dark">Ecossistema</p>
          <h2 id="moura-system-title">O desempenho digital começa na forma como a empresa se torna legível.</h2>
        </div>

        <figure class="moura-system-visual" aria-label="Execução cuidadosa de um trabalho de alto padrão">
          <picture>
            <source srcset="/images/moura/ecosystem-work-excellence.webp" type="image/webp" />
            <img
              src="/images/moura/ecosystem-work-excellence.png"
              alt="Profissional executando um trabalho cuidadoso sobre uma mesa com materiais, plano e anotações"
              loading="lazy"
              decoding="async"
            />
          </picture>

          <figcaption class="moura-system-copy">
            <p>
              Empresas querem aparecer melhor no Google, atrair pessoas qualificadas e transformar interesse em demanda real. O ponto crítico está na origem: a presença precisa tornar legível o valor que a empresa sustenta na prática.
            </p>
            <p>
              Branding, site, conteúdo, busca, reputação e experiência precisam trabalhar como sinais de uma mesma empresa. Quando cada ponto de contato comunica uma versão diferente do negócio, o cliente hesita, o Google classifica com menos precisão e a IA encontra pouco motivo para recomendar.
            </p>
          </figcaption>
        </figure>
      </div>

      <div class="moura-shell moura-pillar-grid" aria-label="Pilares de presença digital">
        {systemPillars.map((pillar, index) => (
          <article class="moura-pillar-card">
            <span>{String(index + 1).padStart(2, '0')} / {pillar.label}</span>
            <h3>{pillar.title}</h3>
            <p>{pillar.text}</p>
          </article>
        ))}
      </div>
    </section>

    <section class="moura-method" data-nav-surface="dark" aria-labelledby="moura-method-title">
      <div class="moura-shell moura-method-grid">
        <div class="moura-section-heading">
          <p class="moura-kicker">Brand-Driven</p>
          <h2 id="moura-method-title">O método começa onde o SEO comum não consegue chegar.</h2>
        </div>

        <div class="moura-method-flow">
          <aside class="moura-method-steps" aria-label="Etapas do método Brand-Driven">
            {methodSteps.map((step, index) => (
              <div class="moura-method-step">
                <small>{String(index + 1).padStart(2, '0')}</small>
                <span>{step.title}</span>
                <p>{step.detail}</p>
              </div>
            ))}
          </aside>
        </div>
      </div>
    </section>

    <section class="moura-badges-section" data-nav-surface="light" aria-labelledby="moura-badges-title">
      <div class="moura-shell moura-badges-heading">
        <p class="moura-kicker moura-kicker-dark">Selos de qualidade</p>
        <h2 id="moura-badges-title">Padrões visíveis para critérios que costumam ficar invisíveis.</h2>
        <p>
          Os selos identificam o nível de exigência aplicado a cada projeto. Eles existem para tornar públicos os critérios que sustentam a entrega: direção estratégica, rigor técnico, leitura editorial, performance, identidade e qualidade estética.
        </p>
      </div>

      <div class="moura-shell moura-badges-grid">
        {qualityBadges.map((standard) => (
          <article class={`moura-badge-card moura-badge-card--${standard.kind}`}>
            <div class="moura-badge-card-head">
              <span>{standard.seal}</span>
              <h3>{standard.expansion}</h3>
              <p>{standard.category}</p>
            </div>

            <a class="moura-badge-stage" href={`/${standard.slug}/`} aria-label={`Conhecer ${standard.seal}`}>
              {standard.kind === 'mhs' && (
                <HSBadge size="large" scale={1.08} />
              )}
              {standard.kind === 'mstar' && <MStarBadge scale={1.08} />}
              {standard.kind === 'mid' && <MIDBadge scale={1.08} />}
              {standard.kind === 'msupreme' && <MSupremeBadge scale={1.08} />}
            </a>

            <p class="moura-badge-description">{standard.description}</p>
            <a class="moura-badge-arrow" href={`/${standard.slug}/`} aria-label={`Conhecer ${standard.seal}`}>
              <svg viewBox="0 0 24 24" aria-hidden="true">
                <path d="M5 12h14M13 6l6 6-6 6" />
              </svg>
            </a>
          </article>
        ))}
      </div>
    </section>

    <section class="moura-distinction" data-nav-surface="dark" aria-labelledby="moura-distinction-title">
      <div class="moura-shell moura-distinction-grid">
        <div class="moura-section-heading">
          <p class="moura-kicker">Critério</p>
          <h2 id="moura-distinction-title">O que diferencia a assinatura.</h2>
        </div>

        <div class="moura-distinction-list">
          {distinctions.map((item, index) => (
            <article>
              <span>{String(index + 1).padStart(2, '0')}</span>
              <h3>{item.title}</h3>
              <p>{item.detail}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="moura-closing" data-nav-surface="light" aria-labelledby="moura-closing-title">
      <div class="moura-shell moura-closing-grid">
        <div class="moura-closing-heading">
          <p class="moura-kicker moura-kicker-dark">Candidatura</p>
          <h2 id="moura-closing-title">Uma presença digital desse nível começa por seleção.</h2>
        </div>
        <div class="moura-closing-copy">
          <p>
            O interesse inicial abre uma análise de compatibilidade entre ambição, entrega real e nível de direção necessário. Orçamentos entram depois, quando fizerem sentido para ambos os lados.
          </p>
          <p>
            A candidatura prioriza empresas e profissionais com substância, maturidade e disposição para tratar marca, site, conteúdo, busca e reputação como parte do mesmo trabalho.
          </p>
          <a href="/candidatura">
          Enviar candidatura</a>
        </div>
      </div>
    </section>
  </main>

  <SiteFooter />
</BaseLayout>

<style>
  .moura-page {
    overflow-x: clip;
    overflow-y: visible;
    background: var(--moura-paper);
    color: var(--moura-ink);
  }

  .moura-shell {
    width: min(100% - clamp(3rem, 8vw, 9rem), 88rem);
    margin-inline: auto;
  }

  .moura-kicker {
    margin: 0;
    font-family: "Neue Haas Display", "Outfit", sans-serif;
    font-size: clamp(0.68rem, 0.8vw, 0.82rem);
    font-weight: 300;
    line-height: 1;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: rgba(244, 239, 231, 0.5);
  }

  .moura-kicker-dark {
    color: rgba(8, 8, 8, 0.46);
  }

  .moura-hero {
    min-height: 100vh;
    display: grid;
    align-items: center;
    padding-block: clamp(7rem, 10vw, 9rem) clamp(4.5rem, 6.5vw, 6rem);
    background:
      radial-gradient(circle at 78% 20%, rgba(167, 124, 61, 0.105), transparent 31%),
      radial-gradient(circle at 18% 78%, rgba(167, 124, 61, 0.055), transparent 34%),
      linear-gradient(145deg, #050505 0%, #0f0f0e 52%, #060606 100%);
    color: var(--moura-ivory);
  }

  .moura-hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 0.86fr) minmax(20rem, 0.46fr);
    gap: clamp(3rem, 7.2vw, 8rem);
    align-items: end;
  }

  .moura-hero-copy,
  .moura-section-heading,
  .moura-badges-heading {
    display: grid;
    gap: clamp(1rem, 2vw, 1.5rem);
  }

  .moura-hero h1,
  .moura-section-heading h2,
  .moura-badges-heading h2,
  .moura-closing h2 {
    margin: 0;
    font-family: "Outfit", "Avenir Next", sans-serif;
    font-weight: 100;
    letter-spacing: 0;
  }

  .moura-hero h1 {
    max-width: 15.6ch;
    font-size: clamp(2.72rem, 5.02vw, 5.9rem);
    line-height: 1.015;
    color: rgba(244, 239, 231, 0.91);
  }

  .moura-hero-aside {
    display: grid;
    gap: clamp(0.78rem, 1.35vw, 1.16rem);
    align-self: end;
    justify-items: start;
  }

  .moura-hero-luxury-mark {
    display: inline-grid;
    place-items: center;
    width: max-content;
    justify-self: center;
    transform: scale(0.88);
    transform-origin: center bottom;
    padding: clamp(0.52rem, 0.9vw, 0.78rem);
    border: 1px solid rgba(167, 124, 61, 0.26);
    background:
      linear-gradient(135deg, rgba(167, 124, 61, 0.065), transparent 42%),
      rgba(4, 4, 4, 0.16);
    box-shadow:
      0 1rem 2.8rem rgba(0, 0, 0, 0.2),
      inset 0 0 1.5rem rgba(167, 124, 61, 0.06);
  }

  .moura-hero-text {
    max-width: 28.5rem;
    margin: 0;
    border-top: 1px solid rgba(244, 239, 231, 0.13);
    padding-top: clamp(0.9rem, 1.45vw, 1.22rem);
    font-family: "Neue Haas Display", "Outfit", sans-serif;
    font-size: clamp(0.96rem, 1.05vw, 1.06rem);
    font-weight: 300;
    line-height: 1.66;
    color: rgba(244, 239, 231, 0.58);
  }

  .moura-hero-system {
    position: absolute;
    right: clamp(3rem, 8vw, 9rem);
    bottom: clamp(2rem, 4vw, 4rem);
    left: clamp(3rem, 8vw, 9rem);
    display: grid;
    grid-template-columns: minmax(14rem, 0.7fr) repeat(3, minmax(10rem, 1fr));
    gap: clamp(1.5rem, 3vw, 3rem);
    align-items: center;
    margin: 0;
    border-top: 1px solid rgba(244, 239, 231, 0.09);
    padding-top: 0.88rem;
    font-family: "Neue Haas Display", "Outfit", sans-serif;
    font-size: 0.66rem;
    font-weight: 300;
    line-height: 1;
    letter-spacing: 0.13em;
    text-transform: uppercase;
    color: rgba(244, 239, 231, 0.34);
  }

  .moura-hero-system p {
    margin: 0;
    color: rgba(167, 124, 61, 0.6);
  }

  .moura-hero-system span {
    display: block;
    text-align: right;
  }

  .moura-system,
  .moura-badges-section,
  .moura-closing {
    padding-block: clamp(7rem, 12vw, 12rem);
    background: var(--moura-paper);
  }

  .moura-closing {
    --closing-overlap: clamp(4.8rem, 9vw, 8rem);
    position: relative;
    z-index: 3;
    min-height: 100vh;
    min-height: 100svh;
    display: grid;
    align-items: center;
    box-sizing: border-box;
    margin-top: 0;
    padding-top: clamp(7rem, 12vw, 12rem);
    overflow: hidden;
    background:
      linear-gradient(180deg, rgba(255, 253, 248, 0.98), var(--moura-paper) 28%, var(--moura-paper) 100%);
    border-top: 1px solid rgba(244, 239, 231, 0.74);
    box-shadow:
      0 -2.2rem 5.4rem rgba(0, 0, 0, 0.22),
      inset 0 1px 0 rgba(255, 255, 255, 0.8);
  }

  .moura-closing::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      radial-gradient(ellipse at 18% 22%, rgba(167, 124, 61, 0.08), transparent 34%),
      linear-gradient(90deg, transparent, rgba(8, 8, 8, 0.035), transparent 52%, rgba(167, 124, 61, 0.035), transparent);
  }

  .moura-closing::after {
    content: "";
    position: absolute;
    inset: clamp(1rem, 2vw, 1.75rem);
    pointer-events: none;
    border-top: 1px solid rgba(8, 8, 8, 0.055);
  }

  .moura-system {
    position: relative;
    overflow: hidden;
    padding-bottom: clamp(10rem, 16vw, 17rem);
  }

  .moura-two-column,
  .moura-method-grid,
  .moura-distinction-grid,
  .moura-closing-grid {
    display: grid;
    grid-template-columns: minmax(0, 0.82fr) minmax(0, 1fr);
    gap: clamp(3rem, 8vw, 8rem);
    align-items: start;
  }

  .moura-closing-grid {
    position: relative;
    z-index: 1;
    align-items: center;
  }

  .moura-closing-heading {
    display: grid;
    gap: clamp(1rem, 2vw, 1.5rem);
  }

  .moura-closing-heading .moura-kicker {
    color: rgba(167, 124, 61, 0.82);
  }

  .moura-section-heading h2,
  .moura-badges-heading h2,
  .moura-closing h2 {
    max-width: 12.8ch;
    font-size: clamp(2.85rem, 5.8vw, 6.7rem);
    line-height: 0.98;
    color: rgba(8, 8, 8, 0.9);
  }

  .moura-system .moura-two-column {
    align-items: center;
  }

  .moura-system .moura-section-heading {
    align-self: center;
  }

  .moura-system-copy,
  .moura-closing-copy {
    display: grid;
    gap: clamp(1.2rem, 2vw, 1.65rem);
    max-width: 42rem;
    font-family: "Neue Haas Display", "Outfit", sans-serif;
    font-size: clamp(1.03rem, 1.22vw, 1.2rem);
    font-weight: 300;
    line-height: 1.68;
    color: rgba(8, 8, 8, 0.64);
  }

  .moura-closing-copy {
    max-width: 34rem;
    margin-left: auto;
    gap: clamp(1.25rem, 2vw, 1.8rem);
    border-left: 1px solid rgba(8, 8, 8, 0.08);
    padding-left: clamp(1.4rem, 2.8vw, 2.4rem);
    color: rgba(8, 8, 8, 0.58);
  }

  .moura-system-copy {
    align-self: center;
    max-width: 35rem;
    margin-left: auto;
    gap: clamp(1.05rem, 1.6vw, 1.45rem);
    font-size: clamp(0.98rem, 1.05vw, 1.08rem);
    font-weight: 300;
    line-height: 1.82;
    color: rgba(8, 8, 8, 0.54);
  }

  .moura-system-copy p,
  .moura-closing-copy p {
    margin: 0;
  }

  .moura-system-visual .moura-system-copy {
    position: absolute;
    z-index: 3;
    left: clamp(-5.5rem, -6vw, -2.5rem);
    bottom: clamp(2.8rem, 5vw, 5.2rem);
    width: min(28rem, 72%);
    max-width: 28rem;
    margin: 0;
    padding: clamp(1.1rem, 1.85vw, 1.58rem);
    border-block: 1px solid rgba(244, 239, 231, 0.16);
    border-inline: 0;
    background: linear-gradient(135deg, rgba(8, 8, 8, 0.52), rgba(8, 8, 8, 0.28));
    box-shadow: 0 0.85rem 2.4rem rgba(8, 8, 8, 0.16);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    color: rgba(244, 239, 231, 0.78);
    text-shadow: 0 1px 18px rgba(8, 8, 8, 0.24);
  }

  .moura-system-visual {
    position: relative;
    min-height: clamp(34rem, 48vw, 46rem);
    margin: 0;
    overflow: visible;
  }

  .moura-system-visual::before {
    content: "";
    position: absolute;
    inset: 0;
    border: 1px solid rgba(8, 8, 8, 0.1);
    pointer-events: none;
    z-index: 2;
  }

  .moura-system-visual::after {
    content: "";
    position: absolute;
    inset: 0;
    background:
      linear-gradient(180deg, rgba(8, 8, 8, 0.04), rgba(8, 8, 8, 0.34)),
      linear-gradient(90deg, rgba(8, 8, 8, 0.22), transparent 42%, rgba(167, 124, 61, 0.05));
    pointer-events: none;
    z-index: 1;
  }

  .moura-system-visual picture,
  .moura-system-visual img {
    display: block;
    width: 100%;
    height: 100%;
  }

  .moura-system-visual picture {
    position: absolute;
    inset: 0;
  }

  .moura-system-visual img {
    object-fit: cover;
    object-position: center;
    filter: saturate(0.8) contrast(1.08) brightness(0.9);
  }

  .moura-pillar-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1px;
    position: relative;
    z-index: 2;
    margin-top: clamp(5rem, 9vw, 8rem);
    background: rgba(8, 8, 8, 0.08);
    border-block: 1px solid rgba(8, 8, 8, 0.1);
  }

  .moura-pillar-card {
    min-height: clamp(11.8rem, 14vw, 14.4rem);
    display: grid;
    align-content: space-between;
    gap: clamp(1.1rem, 1.55vw, 1.55rem);
    padding: clamp(1rem, 1.35vw, 1.36rem);
    background: rgba(255, 253, 248, 0.58);
    transition: background 0.36s cubic-bezier(0.16, 1, 0.3, 1), color 0.36s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .moura-pillar-card:hover {
    background: rgba(255, 253, 248, 0.76);
  }

  .moura-pillar-card span,
  .moura-badge-card-head span,
  .moura-distinction-list span {
    font-family: "Neue Haas Display", "Outfit", sans-serif;
    font-size: clamp(0.68rem, 0.8vw, 0.82rem);
    font-weight: 300;
    line-height: 1;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: rgba(167, 124, 61, 0.72);
  }

  .moura-distinction-list span {
    color: rgba(167, 124, 61, 0.88);
    text-shadow: 0 0 18px rgba(167, 124, 61, 0.08);
  }

  .moura-pillar-card h3,
  .moura-badge-card h3 {
    margin: 0;
    font-family: "Outfit", "Avenir Next", sans-serif;
    font-size: clamp(1.28rem, 1.82vw, 2.12rem);
    font-weight: 100;
    line-height: 1;
    color: rgba(8, 8, 8, 0.84);
  }

  .moura-pillar-card p,
  .moura-badge-card p {
    margin: 0;
    font-family: "Neue Haas Display", "Outfit", sans-serif;
    font-size: clamp(0.82rem, 0.86vw, 0.9rem);
    font-weight: 300;
    line-height: 1.55;
    color: rgba(8, 8, 8, 0.54);
  }

  .moura-method,
  .moura-distinction {
    min-height: 100vh;
    min-height: 100svh;
    display: grid;
    align-items: center;
    padding-block: clamp(7rem, 12vw, 12rem);
    background-color: #050505;
    background:
      radial-gradient(circle at 18% 20%, rgba(167, 124, 61, 0.08), transparent 30%),
      linear-gradient(145deg, #050505 0%, #0f0f0e 58%, #050505 100%);
    color: var(--moura-ivory);
  }

  .moura-method .moura-section-heading h2,
  .moura-distinction .moura-section-heading h2 {
    color: rgba(244, 239, 231, 0.9);
  }

  .moura-method .moura-section-heading h2 {
    max-width: 13.4ch;
    font-size: clamp(2.75rem, 5.25vw, 5.8rem);
    line-height: 1.015;
  }

  .moura-method {
    position: relative;
    align-content: center;
    padding-block: clamp(5rem, 8vw, 9rem);
  }

  .moura-method-flow {
    display: grid;
    align-self: center;
    width: min(100%, 36rem);
    margin-left: auto;
  }

  .moura-method-steps {
    width: min(100%, 29rem);
    display: grid;
    gap: clamp(1.28rem, 1.9vw, 1.72rem);
    margin: 0 0 0 clamp(2rem, 4vw, 4rem);
    padding-left: clamp(1.2rem, 2vw, 1.8rem);
    border-left: 1px solid rgba(244, 239, 231, 0.16);
  }

  .moura-method-step {
    position: relative;
    display: grid;
    gap: 0.4rem;
  }

  .moura-method-step::before {
    content: "";
    position: absolute;
    left: calc(-1 * clamp(1.2rem, 2vw, 1.8rem) - 1px);
    top: 0.48rem;
    width: clamp(0.5rem, 0.7vw, 0.7rem);
    height: 1px;
    background: rgba(167, 124, 61, 0.7);
  }

  .moura-method-step small {
    font-family: "Neue Haas Display", "Outfit", sans-serif;
    font-size: 0.68rem;
    font-weight: 300;
    letter-spacing: 0.18em;
    line-height: 1;
    color: rgba(167, 124, 61, 0.78);
  }

  .moura-method-step span {
    font-family: "Outfit", "Avenir Next", sans-serif;
    font-size: clamp(1.15rem, 1.52vw, 1.58rem);
    font-weight: 100;
    line-height: 1.05;
    color: rgba(244, 239, 231, 0.78);
  }

  .moura-method-step p {
    margin: 0;
    font-family: "Neue Haas Display", "Outfit", sans-serif;
    font-size: clamp(0.82rem, 0.9vw, 0.94rem);
    font-weight: 300;
    line-height: 1.48;
    color: rgba(244, 239, 231, 0.44);
  }

  .moura-badges-heading {
    max-width: 62rem;
  }

  .moura-badges-heading p:not(.moura-kicker) {
    max-width: 40rem;
    margin: 0;
    font-family: "Neue Haas Display", "Outfit", sans-serif;
    font-size: clamp(1rem, 1.18vw, 1.14rem);
    font-weight: 300;
    line-height: 1.62;
    color: rgba(8, 8, 8, 0.58);
  }

  .moura-badges-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1px;
    margin-top: clamp(3.4rem, 6vw, 5.8rem);
    border-block: 1px solid rgba(8, 8, 8, 0.075);
    background:
      linear-gradient(90deg, transparent, rgba(8, 8, 8, 0.085), transparent),
      rgba(8, 8, 8, 0.055);
  }

  .moura-badge-card {
    position: relative;
    min-height: clamp(32rem, 42vw, 39rem);
    display: grid;
    grid-template-rows: clamp(6.25rem, 7.2vw, 6.9rem) 14rem 1fr auto;
    align-content: start;
    gap: clamp(1rem, 1.58vw, 1.5rem);
    padding: clamp(1.15rem, 1.72vw, 1.68rem);
    overflow: hidden;
    background:
      radial-gradient(ellipse at 50% 41%, rgba(167, 124, 61, 0.12), transparent 36%),
      linear-gradient(180deg, rgba(255, 253, 248, 0.84), rgba(255, 253, 248, 0.62)),
      rgba(255, 253, 248, 0.74);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.72),
      inset 0 -1px 0 rgba(8, 8, 8, 0.035);
    transition:
      background 0.42s cubic-bezier(0.16, 1, 0.3, 1),
      box-shadow 0.42s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .moura-badge-card::before {
    content: "";
    position: absolute;
    inset: clamp(0.52rem, 0.8vw, 0.8rem);
    pointer-events: none;
    border: 1px solid rgba(8, 8, 8, 0.035);
    box-shadow: inset 0 0 0 1px rgba(255, 253, 248, 0.34);
  }

  .moura-badge-card::after {
    content: "";
    position: absolute;
    left: 18%;
    right: 18%;
    top: 39%;
    height: 1px;
    pointer-events: none;
    background: linear-gradient(90deg, transparent, rgba(167, 124, 61, 0.22), transparent);
    opacity: 0.62;
  }

  .moura-badge-card:hover {
    background:
      radial-gradient(ellipse at 50% 41%, rgba(167, 124, 61, 0.17), transparent 39%),
      linear-gradient(180deg, rgba(255, 253, 248, 0.9), rgba(255, 253, 248, 0.68)),
      rgba(255, 253, 248, 0.82);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.82),
      inset 0 -1px 0 rgba(8, 8, 8, 0.04),
      0 1rem 2.4rem rgba(8, 8, 8, 0.045);
  }

  .moura-badge-card-head {
    position: relative;
    z-index: 1;
    display: grid;
    align-content: start;
    gap: 0.56rem;
  }

  .moura-badge-card-head span {
    color: rgba(167, 124, 61, 0.76);
  }

  .moura-badge-card-head h3 {
    max-width: 12ch;
    font-size: clamp(1.18rem, 1.72vw, 2.04rem);
    color: rgba(8, 8, 8, 0.78);
  }

  .moura-badge-card-head p {
    color: rgba(8, 8, 8, 0.42);
  }

  .moura-badge-stage {
    position: relative;
    z-index: 1;
    display: grid;
    height: 14rem;
    min-height: 14rem;
    place-items: center;
    align-self: start;
    color: inherit;
    text-decoration: none;
  }

  .moura-badge-stage::before {
    content: "";
    position: absolute;
    width: min(72%, 12rem);
    aspect-ratio: 1 / 0.32;
    bottom: 1.1rem;
    border-radius: 999px;
    background: radial-gradient(ellipse at center, rgba(167, 124, 61, 0.18), rgba(167, 124, 61, 0.06) 42%, transparent 72%);
    filter: blur(0.22rem);
    opacity: 0.72;
    transform: translateY(0.2rem);
    transition:
      opacity 0.42s cubic-bezier(0.16, 1, 0.3, 1),
      transform 0.42s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .moura-badge-stage::after {
    content: "";
    position: absolute;
    inset: 0.5rem 0;
    pointer-events: none;
    background:
      linear-gradient(180deg, transparent 0%, rgba(255, 253, 248, 0.24) 49%, transparent 50%),
      radial-gradient(ellipse at 50% 50%, rgba(255, 253, 248, 0.2), transparent 56%);
    opacity: 0.46;
    transition: opacity 0.42s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .moura-badge-stage > * {
    position: relative;
    z-index: 1;
    filter: drop-shadow(0 0.7rem 1.2rem rgba(8, 8, 8, 0.08));
    transition:
      filter 0.42s cubic-bezier(0.16, 1, 0.3, 1),
      transform 0.42s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .moura-badge-card:hover .moura-badge-stage::before {
    opacity: 1;
    transform: translateY(0);
  }

  .moura-badge-card:hover .moura-badge-stage::after {
    opacity: 0.64;
  }

  .moura-badge-card:hover .moura-badge-stage > * {
    filter: drop-shadow(0 0.9rem 1.5rem rgba(8, 8, 8, 0.1));
    transform: translateY(-0.08rem);
  }

  .moura-badge-description {
    position: relative;
    z-index: 1;
    align-self: start;
    max-width: 28ch;
    color: rgba(8, 8, 8, 0.5);
  }

  .moura-badge-arrow {
    position: relative;
    z-index: 1;
    display: inline-grid;
    width: 2.75rem;
    height: 2.75rem;
    place-items: center;
    align-self: end;
    justify-self: end;
    border: 1px solid rgba(8, 8, 8, 0.15);
    border-radius: 999px;
    background: rgba(255, 253, 248, 0.2);
    color: rgba(8, 8, 8, 0.64);
    text-decoration: none;
    transition:
      border-color 0.36s cubic-bezier(0.16, 1, 0.3, 1),
      color 0.36s cubic-bezier(0.16, 1, 0.3, 1),
      transform 0.36s cubic-bezier(0.16, 1, 0.3, 1),
      background 0.36s cubic-bezier(0.16, 1, 0.3, 1),
      box-shadow 0.36s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .moura-badge-arrow svg {
    width: 1rem;
    height: 1rem;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.5;
  }

  .moura-badge-arrow:hover {
    border-color: rgba(167, 124, 61, 0.5);
    background: rgba(255, 253, 248, 0.46);
    box-shadow: 0 0.5rem 1.4rem rgba(167, 124, 61, 0.08);
    color: var(--moura-gold);
    transform: translate(0.1rem, -0.1rem);
  }

  .moura-distinction {
    position: sticky;
    top: 0;
    z-index: 1;
    isolation: isolate;
    min-height: 100vh;
    min-height: 100svh;
    box-sizing: border-box;
    display: grid;
    align-items: center;
    overflow: hidden;
    padding-block: clamp(6rem, 9vw, 10rem);
    background-color: #050505;
    color: var(--moura-ivory);
  }

  .moura-distinction::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: -2;
    min-height: 100vh;
    min-height: 100svh;
    background:
      radial-gradient(ellipse at 17% 18%, rgba(167, 124, 61, 0.16), transparent 32%),
      radial-gradient(ellipse at 72% 86%, rgba(244, 239, 231, 0.045), transparent 34%),
      linear-gradient(150deg, #040404 0%, #0d0d0c 48%, #050505 100%);
  }

  .moura-distinction::after {
    content: "";
    position: absolute;
    inset: clamp(1rem, 2vw, 1.75rem);
    z-index: -1;
    pointer-events: none;
    border-block: 1px solid rgba(244, 239, 231, 0.045);
    background:
      linear-gradient(90deg, transparent, rgba(244, 239, 231, 0.028), transparent),
      linear-gradient(180deg, rgba(255, 253, 248, 0.018), transparent 22%, transparent 76%, rgba(167, 124, 61, 0.025));
  }

  .moura-distinction-grid {
    position: relative;
    z-index: 1;
    align-items: center;
  }

  .moura-distinction-list {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1px;
    border-block: 1px solid rgba(244, 239, 231, 0.095);
    background:
      linear-gradient(90deg, transparent, rgba(244, 239, 231, 0.09), transparent),
      rgba(244, 239, 231, 0.045);
    box-shadow:
      0 1rem 3rem rgba(0, 0, 0, 0.14),
      inset 0 1px 0 rgba(255, 253, 248, 0.025);
  }

  .moura-distinction-list article {
    min-height: clamp(12.4rem, 15.2vw, 15.8rem);
    display: grid;
    grid-template-rows: auto auto 1fr;
    gap: clamp(0.8rem, 1.22vw, 1.12rem);
    padding: clamp(1.25rem, 1.82vw, 1.68rem);
    background:
      linear-gradient(135deg, rgba(244, 239, 231, 0.025), transparent 46%),
      rgba(8, 8, 8, 0.18);
    box-shadow: inset 0 1px 0 rgba(244, 239, 231, 0.018);
    transition:
      background 0.36s cubic-bezier(0.16, 1, 0.3, 1),
      box-shadow 0.36s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .moura-distinction-list article:hover {
    background:
      linear-gradient(135deg, rgba(167, 124, 61, 0.045), transparent 50%),
      rgba(8, 8, 8, 0.25);
    box-shadow: inset 0 1px 0 rgba(244, 239, 231, 0.03);
  }

  .moura-distinction-list h3 {
    max-width: 13ch;
    margin: 0;
    font-family: "Outfit", "Avenir Next", sans-serif;
    font-size: clamp(1.34rem, 2.05vw, 2.42rem);
    font-weight: 100;
    line-height: 1.02;
    color: rgba(244, 239, 231, 0.82);
  }

  .moura-distinction-list p {
    align-self: end;
    max-width: 24rem;
    margin: 0;
    font-family: "Neue Haas Display", "Outfit", sans-serif;
    font-size: clamp(0.84rem, 0.88vw, 0.93rem);
    font-weight: 300;
    line-height: 1.62;
    color: rgba(244, 239, 231, 0.5);
  }

  .moura-closing-copy a {
    width: fit-content;
    margin-top: clamp(1.2rem, 2.4vw, 2rem);
    border: 1px solid rgba(8, 8, 8, 0.2);
    padding: 1rem 1.25rem;
    font-family: "Outfit", "Avenir Next", sans-serif;
    font-size: 0.78rem;
    font-weight: 300;
    letter-spacing: 0.14em;
    line-height: 1;
    text-transform: uppercase;
    color: rgba(8, 8, 8, 0.86);
    text-decoration: none;
    transition: border-color 0.36s cubic-bezier(0.16, 1, 0.3, 1), color 0.36s cubic-bezier(0.16, 1, 0.3, 1), transform 0.36s cubic-bezier(0.16, 1, 0.3, 1), background 0.36s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .moura-closing-copy a:hover {
    border-color: rgba(167, 124, 61, 0.62);
    background: rgba(167, 124, 61, 0.04);
    color: var(--moura-gold);
    transform: translateX(0.18rem);
  }

  @media (max-width: 1180px) {
    .moura-distinction {
      position: relative;
      top: auto;
    }

    .moura-hero-aside {
      margin-bottom: 0;
    }

    .moura-system-visual {
      min-height: clamp(30rem, 64vw, 40rem);
    }

    .moura-system-visual .moura-system-copy {
      left: clamp(1.25rem, 4vw, 2.5rem);
      bottom: clamp(1.25rem, 4vw, 2.5rem);
      width: calc(100% - clamp(2.5rem, 8vw, 5rem));
      max-width: 32rem;
      border-inline: 0;
    }

    .moura-system-copy,
    .moura-closing-copy {
      max-width: 42rem;
      margin-left: 0;
    }

    .moura-closing-copy {
      border-left: 0;
      padding-left: 0;
    }

    .moura-method-flow {
      width: min(100%, 42rem);
      margin-left: 0;
    }

    .moura-method-steps {
      width: min(100%, 36rem);
      margin-left: clamp(1rem, 3vw, 2rem);
    }

    .moura-pillar-grid,
    .moura-badges-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .moura-distinction-list {
      grid-template-columns: 1fr;
    }

    .moura-hero-aside {
      max-width: 31rem;
    }

    .moura-hero-grid,
    .moura-two-column,
    .moura-method-grid,
    .moura-distinction-grid,
    .moura-closing-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 767px) {
    .moura-shell {
      width: min(100% - 3rem, 88rem);
    }

    .moura-hero-system {
      display: none;
    }

    .moura-hero h1,
    .moura-section-heading h2,
    .moura-badges-heading h2,
    .moura-closing h2 {
      font-size: clamp(2.35rem, 10.8vw, 3.75rem);
      line-height: 1;
    }

    .moura-closing {
      --closing-overlap: 0rem;
      padding-bottom: 6.4rem;
    }

    .moura-hero {
      padding-block: 7.25rem 4.4rem;
      background:
        radial-gradient(circle at 76% 20%, rgba(167, 124, 61, 0.08), transparent 34%),
        linear-gradient(145deg, #050505 0%, #0f0f0e 54%, #060606 100%);
    }

    .moura-hero-grid {
      gap: 2.6rem;
      align-items: start;
    }

    .moura-hero h1 {
      max-width: 13.8ch;
      font-size: clamp(2.42rem, 10.2vw, 3.22rem);
      line-height: 1.035;
    }

    .moura-hero-aside {
      max-width: 27rem;
      gap: 0.95rem;
    }

    .moura-hero-luxury-mark {
      justify-self: start;
      transform: scale(0.72);
      transform-origin: left bottom;
      padding: 0.48rem;
      border-color: rgba(167, 124, 61, 0.22);
      box-shadow: 0 0.8rem 2rem rgba(0, 0, 0, 0.18);
    }

    .moura-hero-text {
      max-width: 22.5rem;
      border-top-color: rgba(244, 239, 231, 0.11);
      padding-top: 1rem;
      font-size: 0.96rem;
      line-height: 1.62;
      color: rgba(244, 239, 231, 0.58);
    }

    .moura-system {
      padding-block: 5.8rem 7rem;
    }

    .moura-system .moura-section-heading h2 {
      max-width: 12.5ch;
      font-size: clamp(2.12rem, 9.1vw, 3rem);
      line-height: 1.04;
    }

    .moura-system-visual {
      min-height: 31rem;
    }

    .moura-system-visual .moura-system-copy {
      left: 0.9rem;
      bottom: 0.9rem;
      width: calc(100% - 1.8rem);
      padding: 1rem;
      font-size: 0.9rem;
      line-height: 1.62;
      background: rgba(8, 8, 8, 0.5);
      box-shadow: 0 0.75rem 1.8rem rgba(8, 8, 8, 0.14);
      backdrop-filter: blur(7px);
      -webkit-backdrop-filter: blur(7px);
    }

    .moura-pillar-grid {
      margin-top: 4.6rem;
    }

    .moura-method {
      padding-block: 6.2rem 6.6rem;
    }

    .moura-method .moura-section-heading h2 {
      max-width: 13ch;
      font-size: clamp(2.16rem, 9vw, 3rem);
      line-height: 1.04;
    }

    .moura-method-flow {
      width: 100%;
      margin-left: 0;
    }

    .moura-method-steps {
      width: 100%;
      gap: 1.18rem;
      margin-left: 0.8rem;
      padding-left: 1rem;
      border-left-color: rgba(244, 239, 231, 0.12);
    }

    .moura-method-step::before {
      left: calc(-1rem - 1px);
      top: 0.42rem;
      width: 0.48rem;
      background: rgba(167, 124, 61, 0.58);
    }

    .moura-method-step span {
      font-size: clamp(1.04rem, 4.8vw, 1.3rem);
      line-height: 1.08;
    }

    .moura-pillar-grid,
    .moura-badges-grid {
      grid-template-columns: 1fr;
    }

    .moura-badge-card {
      min-height: auto;
    }

    .moura-badge-arrow {
      justify-self: start;
    }

    .moura-pillar-card,
    .moura-badge-card {
      min-height: 20rem;
    }

    .moura-system .moura-pillar-card {
      min-height: auto;
      gap: 1.25rem;
      padding: 1.1rem;
    }

    .moura-system .moura-pillar-card h3 {
      font-size: clamp(1.34rem, 6vw, 1.9rem);
      line-height: 1.04;
    }

    .moura-system .moura-pillar-card p {
      font-size: 0.86rem;
      line-height: 1.54;
    }
  }
</style>
